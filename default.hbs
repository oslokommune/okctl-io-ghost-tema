<!DOCTYPE html>
<html lang="{{@site.locale}}">
	<head>
		{{! Document Settings }}
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0 shrink-to-fit=no">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
		{{! Page Meta }}
		<title>{{#is "faq"}}{{#if tag.meta_title}}{{tag.meta_title}}{{else}}{{tag.name}}{{/if}} - {{@site.title}}{{else}}{{meta_title}}{{/is}}</title>
		{{! Styles'n'Scripts }}
		<link href="https://fonts.googleapis.com/css?family=Lato:400,400i,700,700i&display=swap" rel="stylesheet">
		<link href="{{asset "css/screen.min.css"}}" rel="stylesheet">
		{{! Ghost outputs important style and meta data with this tag }}
		{{ghost_head}}
	</head>
	<body class="{{body_class}}">
		<div class="site-wrap">
		{{! insert header partial}}
		{{> header}}
        {{{body}}}
        {{! insert footer partial}}
        {{> footer}}
		{{! scripts}}
		</div>
		
        <script>
			var apiKey = '{{>api-key}}';
            var nextPage = '{{pagination.next}}';
            var totalPages = '{{pagination.pages}}';
			var loadingText ='{{t "Loading..."}}';
        </script>
		
		<script src="{{asset "js/app.bundle.min.js"}}"></script>
        {{ghost_foot}}
	</body>
</html>